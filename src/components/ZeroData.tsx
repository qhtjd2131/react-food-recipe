import React from "react";
import styled from "styled-components";

const ZeroDataBox = styled.div`
  font-size: 2rem;
`;
const ZeroData = () => {
  return <ZeroDataBox>검색 결과가 없습니다.</ZeroDataBox>;
};

export default ZeroData;
